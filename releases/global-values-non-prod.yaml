# Global configuration for all applications
#
parent: false
env: non-prod
project: tooling

source:
  server: https://kubernetes.default.svc
  namespace: cd


destination:
  server: https://kubernetes.default.svc
  namespace: tooling

