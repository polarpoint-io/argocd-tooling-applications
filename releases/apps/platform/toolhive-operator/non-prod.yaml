chart:
  name: toolhive-operator
  url: ghcr.io/stacklok/toolhive
  targetRevision: 0.41.0
  namespace: toolhive-system

labels:
  service: tooling
  app: toolhive

values: |
  operator:
    replicaCount: 1
