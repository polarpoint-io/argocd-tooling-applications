# TLS termination for the tailscale L4 device 'mcp' -> toolhive-registry (http)
apiVersion: v1
kind: ConfigMap
metadata:
  name: mcp-tls-proxy
  namespace: toolhive-system
data:
  nginx.conf: |
    events {}
    http {
      server {
        listen 8443 ssl;
        server_name {{ .Values.host }};
        ssl_certificate /tls/tls.crt;
        ssl_certificate_key /tls/tls.key;
        client_max_body_size 100m;
        location / {
          proxy_pass http://{{ .Values.registryService }}:8080;
          proxy_set_header Host $host;
          proxy_set_header X-Forwarded-Proto https;
          proxy_set_header Upgrade $http_upgrade;
          proxy_set_header Connection "upgrade";
          proxy_http_version 1.1;
          proxy_read_timeout 3600;
        }
      }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-tls-proxy
  namespace: toolhive-system
spec:
  replicas: 1
  selector:
    matchLabels: {app: mcp-tls-proxy}
  template:
    metadata:
      labels: {app: mcp-tls-proxy}
    spec:
      containers:
        - name: nginx
          image: nginx:1.27-alpine
          ports: [{containerPort: 8443}]
          volumeMounts:
            - {name: conf, mountPath: /etc/nginx/nginx.conf, subPath: nginx.conf}
            - {name: tls, mountPath: /tls, readOnly: true}
      volumes:
        - name: conf
          configMap: {name: mcp-tls-proxy}
        - name: tls
          secret: {secretName: mcp-tls}
---
apiVersion: v1
kind: Service
metadata:
  name: mcp
  namespace: toolhive-system
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: mcp
spec:
  selector: {app: mcp-tls-proxy}
  ports:
    - {name: https, port: 443, targetPort: 8443}
