apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: postiz-tls
  namespace: postiz
spec:
  secretName: postiz-tls
  issuerRef:
    name: {{ .Values.issuer }}
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.host }}
